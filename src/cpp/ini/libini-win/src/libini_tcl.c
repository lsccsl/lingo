/* ----------------------------------------------------------------------------
 * This file was automatically generated by SWIG (http://www.swig.org).
 * Version 1.3u-20010419-1559 (Alpha 5)
 * 
 * This file is not intended to be easily readable and contains a number of 
 * coding conventions designed to improve portability and efficiency. Do not make
 * changes to this file unless you know what you are doing--modify the SWIG 
 * interface file instead. 
 * ----------------------------------------------------------------------------- */

/***********************************************************************
 * common.swg
 *
 *     This file contains generic SWIG runtime support for pointer
 *     type checking as well as a few commonly used macros to control
 *     external linkage.
 *
 * Author : David Beazley (beazley@cs.uchicago.edu)
 *
 * Copyright (c) 1999-2000, The University of Chicago
 * 
 * This file may be freely redistributed without license or fee provided
 * this copyright message remains intact.
 ************************************************************************/

#include <string.h>

#if defined(_WIN32) || defined(__WIN32__)
#       if defined(_MSC_VER)
#               if defined(STATIC_LINKED)
#                       define SWIGEXPORT(a) a
#               else
#                       define SWIGEXPORT(a) __declspec(dllexport) a
#               endif
#       else
#               if defined(__BORLANDC__)
#                       define SWIGEXPORT(a) a _export
#               else
#                       define SWIGEXPORT(a) a
#       endif
#endif
#else
#       define SWIGEXPORT(a) a
#endif

#ifdef SWIG_GLOBAL
#define SWIGRUNTIME(a) SWIGEXPORT(a)
#else
#define SWIGRUNTIME(a) static a
#endif

#ifdef __cplusplus
extern "C" {
#endif

typedef struct swig_type_info {
  char  *name;                 
  void *(*converter)(void *);
  char  *str;
  struct swig_type_info  *next;
  struct swig_type_info  *prev;
} swig_type_info;

#ifdef SWIG_NOINCLUDE
SWIGEXPORT(swig_type_info *) SWIG_TypeRegister(swig_type_info *);
SWIGEXPORT(swig_type_info *) SWIG_TypeCheck(char *c, swig_type_info *);
SWIGEXPORT(void *) SWIG_TypeCast(swig_type_info *, void *);
#else

static swig_type_info *swig_type_list = 0;

/* Register a type mapping with the type-checking */
SWIGRUNTIME(swig_type_info *)
SWIG_TypeRegister(swig_type_info *ti)
{
  swig_type_info *tc, *head, *ret, *next;
  /* Check to see if this type has already been registered */
  tc = swig_type_list;
  while (tc) {
    if (strcmp(tc->name, ti->name) == 0) {
      /* Already exists in the table.  Just add additional types to the list */
      head = tc;
      next = tc->next;
      goto l1;
    }
    tc = tc->prev;
  }
  head = ti;
  next = 0;

  /* Place in list */
  ti->prev = swig_type_list;
  swig_type_list = ti;

  /* Build linked lists */
 l1:
  ret = head;
  tc = ti + 1;
  /* Patch up the rest of the links */
  while (tc->name) {
    head->next = tc;
    tc->prev = head;
    head = tc;
    tc++;
  }
  head->next = next;
  return ret;
}

/* Check the typename */
SWIGRUNTIME(swig_type_info *) 
SWIG_TypeCheck(char *c, swig_type_info *ty)
{
  swig_type_info *s;
  if (!ty) return 0;        /* Void pointer */
  s = ty->next;             /* First element always just a name */
  while (s) {
    if (strcmp(s->name,c) == 0) {
      if (s == ty->next) return s;
      /* Move s to the top of the linked list */
      s->prev->next = s->next;
      if (s->next) {
	s->next->prev = s->prev;
      }
      /* Insert s as second element in the list */
      s->next = ty->next;
      if (ty->next) ty->next->prev = s;
      ty->next = s;
      return s;
    }
    s = s->next;
  }
  return 0;
}

/* Cast a pointer (needed for C++ inheritance */
SWIGRUNTIME(void *) 
SWIG_TypeCast(swig_type_info *ty, void *ptr) 
{
  if ((!ty) || (!ty->converter)) return ptr;
  return (*ty->converter)(ptr);
}

/* Search for a swig_type_info structure */
SWIGRUNTIME(void *)
SWIG_TypeQuery(const char *name) {
  swig_type_info *ty = swig_type_list;
  while (ty) {
    if (ty->str && (strcmp(name,ty->str) == 0)) return ty;
    if (ty->name && (strcmp(name,ty->name) == 0)) return ty;
    ty = ty->prev;
  }
  return 0;
}

#endif

#ifdef __cplusplus
}
#endif



/*
 * $Header: /cvsroot/libini/libini/src/libini_tcl.c,v 1.3 2001/04/20 15:46:36 s_a_white Exp $
 * 
 * swigtcl8.swg
 */

#include <tcl.h>
#include <stdlib.h>
#include <stdarg.h>

#ifdef __cplusplus
extern "C" {
#endif

#ifdef SWIG_NOINCLUDE
SWIGEXPORT(int) SWIG_ConvertPtrFromString(Tcl_Interp *, char *, void **, swig_type_info *);
SWIGEXPORT(int) SWIG_ConvertPtr(Tcl_Interp *, Tcl_Obj *, void **, swig_type_info *);
SWIGEXPORT(void) SWIG_MakePtr(char *, void *, swig_type_info *);
SWIGEXPORT(Tcl_Obj *) SWIG_NewPointerObj(void *, swig_type_info *);
SWIGEXPORT(int) SWIG_GetArgs(Tcl_Interp *, int, Tcl_Obj *CONST [], const char *, ...);
SWIGEXPORT(char *) SWIG_PointerTypeFromString(char *c);
#else

/* Convert a pointer value */
SWIGRUNTIME(int)
SWIG_ConvertPtrFromString(Tcl_Interp *interp, char *c, void **ptr, swig_type_info *ty)
{
  unsigned long p;
  register int d;
  swig_type_info *tc;
  p = 0;
  /* Pointer values must start with leading underscore */
  if (*c != '_') {
    *ptr = (void *) 0;
    if (strcmp(c,"NULL") == 0) return TCL_OK;
    Tcl_SetResult(interp,"Type error. Expected a pointer", TCL_STATIC);
    return TCL_ERROR;
  }
  c++;
  /* Extract hex value from pointer */
  while ((d = *c)) {
    if ((d >= '0') && (d <= '9'))
      p = (p << 4) + (d - '0');
    else if ((d >= 'a') && (d <= 'f'))
      p = (p << 4) + (d - ('a'-10));
    else
      break; 
    c++;
  }
  *ptr = (void *) p;
  if (ty) {
    tc = SWIG_TypeCheck(c,ty);
    if (!tc) {
      Tcl_SetResult(interp,"Type error. Expected ", TCL_STATIC);
      Tcl_AppendElement(interp, ty->name);
      return TCL_ERROR;
    }
    *ptr = SWIG_TypeCast(tc,(void *)p);
  }
  return TCL_OK;
}


/* Convert a pointer value */
SWIGRUNTIME(int)
SWIG_ConvertPtr(Tcl_Interp *interp, Tcl_Obj *oc, void **ptr, swig_type_info *ty)
{
  return SWIG_ConvertPtrFromString(interp, Tcl_GetStringFromObj(oc,NULL), ptr, ty);
}


/* Convert a pointer value */
SWIGRUNTIME(char *)
SWIG_PointerTypeFromString(char *c) {
  char d;
  /* Pointer values must start with leading underscore */
  if (*c != '_') {
    return 0;
  }
  c++;
  /* Extract hex value from pointer */
  while (d = *c) {
    if (!(((d >= '0') && (d <= '9')) || ((d >= 'a') && (d <= 'f')))) break;
    c++;
  }
  return c;
}

/* Take a pointer and convert it to a string */
SWIGRUNTIME(void) 
SWIG_MakePtr(char *c, void *ptr, swig_type_info *ty) {
  static char hex[17] = "0123456789abcdef";
  unsigned long p, s;
  char result[24], *r; 
  r = result;
  p = (unsigned long) ptr;
  if (p > 0) {
    while (p > 0) {
      s = p & 0xf;
      *(r++) = hex[s];
      p = p >> 4;
    }
    *r = '_';
    while (r >= result)
      *(c++) = *(r--);
    strcpy (c, ty->name);
  } else {
    strcpy (c, "NULL");
  }
}

/* Create a new pointer object */
SWIGRUNTIME(Tcl_Obj *)
SWIG_NewPointerObj(void *ptr, swig_type_info *type) {
  char result[256];
  Tcl_Obj *robj;
  SWIG_MakePtr(result,ptr,type);
  robj = Tcl_NewStringObj(result,-1);
  return robj;
}

/* Get arguments */
SWIGRUNTIME(int)
SWIG_GetArgs(Tcl_Interp *interp, int objc, Tcl_Obj *CONST objv[], const char *fmt, ...)
{
  int        argno = 0, opt = 0, tempi;
  int        us = 0;
  double     tempd;
  const char *c;
  va_list    ap;
  void      *vptr;
  swig_type_info *ty;

  va_start(ap,fmt);
  for (c = fmt; (*c && (*c != ':')); c++,argno++) {
    if (*c == '|') {
      opt = 1;
      c++;
    }
    if (argno >= (objc-1)) {
      if (!opt) {
	Tcl_SetResult(interp,"Wrong # args. ", TCL_STATIC);
	goto argerror;
      } else {
	va_end(ap);
	return TCL_OK;
      }
    }
    vptr = va_arg(ap,void *);
    if (vptr) {
      us = 0;
      if (*c == 'u') {
	us = 1;
	c++;
      }
      switch(*c) {
      case 'i':
      case 'l':
      case 'h':
      case 'b':
	if (Tcl_GetIntFromObj(interp,objv[argno+1],&tempi) != TCL_OK) goto argerror;
	if (*c == 'i') *((int *)vptr) = tempi;
	else if (*c == 'l') *((long *)vptr) = tempi;
	else if (*c == 'h') *((short*)vptr) = tempi;
	else if (*c == 'b') *((unsigned char *)vptr) = tempi;
	break;
      case 'f':
      case 'd':
	if (Tcl_GetDoubleFromObj(interp,objv[argno+1],&tempd) != TCL_OK) goto argerror;
	if (*c == 'f') *((float *) vptr) = tempd;
	else if (*c == 'd') *((double*) vptr) = tempd;
	break;
      case 's':
	if (*(c+1) == '#') {
	  int *vlptr = (int *) va_arg(ap, void *);
	  *((char **) vptr) = Tcl_GetStringFromObj(objv[argno+1], vlptr);
	  c++;
	} else {
	  *((char **)vptr) = Tcl_GetStringFromObj(objv[argno+1],NULL);
	}
	break;
      case 'c':
	*((char *)vptr) = *(Tcl_GetStringFromObj(objv[argno+1],NULL));
	break;
      case 'p':
	ty = (swig_type_info *) va_arg(ap, void *);
	if (SWIG_ConvertPtr(interp, objv[argno+1], (void **) vptr, ty) == TCL_ERROR) goto argerror;
	break;
      case 'o':
	*((Tcl_Obj **)vptr) = objv[argno+1];
	break;
      default:
	break;
      }
    }
  }
  if ((objc-1) > argno) {
    Tcl_SetResult(interp,"Wrong # args.", TCL_STATIC);
    goto argerror;
  }
  va_end(ap);
  return TCL_OK;
  
 argerror:
  {
    char temp[32];
    sprintf(temp,"%d", argno);
    Tcl_AppendResult(interp,strchr(fmt,':'), " argument ", temp, NULL);
    va_end(ap);
    return TCL_ERROR;
  }
}

#endif

/* Structure for command table */
typedef struct {
  const char *name;
  int       (*wrapper)(ClientData, Tcl_Interp *, int, Tcl_Obj *CONST []);
  ClientData  clientdata;
} swig_command_info;

/* Structure for variable linking table */
typedef struct {
  const char *name;
  void *addr;
  char * (*get)(ClientData, Tcl_Interp *, char *, char *, int);
  char * (*set)(ClientData, Tcl_Interp *, char *, char *, int);
} swig_var_info;


#ifdef __cplusplus
}
#endif


/* -------- TYPES TABLE (BEGIN) -------- */

#define  SWIGTYPE_ini_fd_t swig_types[0] 
#define  SWIGTYPE_p_ini_buffer_t swig_types[1] 
static swig_type_info *swig_types[3];

/* -------- TYPES TABLE (END) -------- */

#define SWIG_init    Libini_Init
#define SWIG_name    "libini"
#define SWIG_prefix  ""
#define SWIG_namespace ""

#ifdef __cplusplus
extern "C" {
#endif
#ifdef MAC_TCL
#pragma export on
#endif
SWIGEXPORT(int) Libini_Init(Tcl_Interp *);
#ifdef MAC_TCL
#pragma export off
#endif
#ifdef __cplusplus
}
#endif

#include "libini.h"

typedef struct 
{
    char  *buffer;
    size_t size;
} ini_buffer_t;

ini_buffer_t *ini_createBuffer (unsigned int size)
{
    ini_buffer_t *b;
    /* Allocate memory to structure */
    if (size == ( ((unsigned) -1 << 1) >> 1 ))
        return 0; /* Size is too big */
    b = malloc (sizeof (ini_buffer_t));
    if (!b)
        return 0;

    /* Allocate memory to buffer */
    b->buffer = malloc (sizeof (char) * (size + 1));
    if (!b->buffer)
    {
        free (b);
        return 0;
    }
    b->size = size;

    /* Returns address to tcl */
    return b;
}

void ini_deleteBuffer (ini_buffer_t *buffer)
{
    if (!buffer)
        return;
    free (buffer->buffer);
    free (buffer);
}

/*************************************************************
 * SWIG helper functions to create C compatible string buffers
 *************************************************************/
int ini_readFileToBuffer (ini_fd_t fd, ini_buffer_t *buffer)
{
    if (!buffer)
        return -1;
    return ini_readString (fd, buffer->buffer, buffer->size + 1);
}

int ini_writeFileFromBuffer (ini_fd_t fd, ini_buffer_t *buffer)
{
    if (!buffer)
        return -1;
    return ini_writeString (fd, buffer->buffer);
}

char *ini_getBuffer (ini_buffer_t *buffer)
{
    if (!buffer)
        return "";
    return buffer->buffer;
}

int ini_setBuffer (ini_buffer_t *buffer, char *str)
{
    size_t len;
    if (!buffer)
        return -1;
    len = strlen (str);
    if (len > buffer->size)
        len = buffer->size;

    memcpy (buffer->buffer, str, len);
    buffer->buffer[len] = '\0';
    return len;
}

extern ini_fd_t ini_new(char *);
extern ini_fd_t ini_open(char *);
extern int ini_close(ini_fd_t );
extern int ini_flush(ini_fd_t );
extern int ini_locateKey(ini_fd_t ,char *);
extern int ini_locateHeading(ini_fd_t ,char *);
extern int ini_deleteKey(ini_fd_t );
extern int ini_deleteHeading(ini_fd_t );
extern int ini_dataLength(ini_fd_t );
extern int ini_readInt(ini_fd_t ,int *);
extern int ini_readLong(ini_fd_t ,long *);
extern int ini_readDouble(ini_fd_t ,double *);
extern int ini_writeInt(ini_fd_t ,int );
extern int ini_writeLong(ini_fd_t ,long );
extern int ini_writeDouble(ini_fd_t ,double );
extern int ini_listLength(ini_fd_t );
extern int ini_listDelims(ini_fd_t ,char *);
extern int ini_listIndex(ini_fd_t ,unsigned long );
extern int ini_listIndexLength(ini_fd_t );
static int
_wrap_ini_readString(ClientData clientData, Tcl_Interp *interp, int objc, Tcl_Obj *CONST objv[]) {
    ini_fd_t arg0 ;
    ini_buffer_t *arg1 ;
    int result ;
    
    if (SWIG_GetArgs(interp, objc, objv,"pp:ini_readString fd buffer ",&arg0, SWIGTYPE_ini_fd_t,&arg1, SWIGTYPE_p_ini_buffer_t) == TCL_ERROR) return TCL_ERROR;
    result = (int )ini_readFileToBuffer(arg0,arg1);
    Tcl_SetObjResult(interp,Tcl_NewIntObj((long) result));
    return TCL_OK;
}

static int
_wrap_ini_writeString(ClientData clientData, Tcl_Interp *interp, int objc, Tcl_Obj *CONST objv[]) {
    ini_fd_t arg0 ;
    ini_buffer_t *arg1 ;
    int result ;
    
    if (SWIG_GetArgs(interp, objc, objv,"pp:ini_writeString fd buffer ",&arg0, SWIGTYPE_ini_fd_t,&arg1, SWIGTYPE_p_ini_buffer_t) == TCL_ERROR) return TCL_ERROR;
    result = (int )ini_writeFileFromBuffer(arg0,arg1);
    Tcl_SetObjResult(interp,Tcl_NewIntObj((long) result));
    return TCL_OK;
}

static int
_wrap_ini_createBuffer(ClientData clientData, Tcl_Interp *interp, int objc, Tcl_Obj *CONST objv[]) {
    unsigned long arg0 ;
    ini_buffer_t *result ;
    
    if (SWIG_GetArgs(interp, objc, objv,"l:ini_createBuffer size ",&arg0) == TCL_ERROR) return TCL_ERROR;
    result = (ini_buffer_t *)ini_createBuffer(arg0);
    Tcl_SetObjResult(interp,SWIG_NewPointerObj((void *) result,SWIGTYPE_p_ini_buffer_t));
    return TCL_OK;
}

static int
_wrap_ini_deleteBuffer(ClientData clientData, Tcl_Interp *interp, int objc, Tcl_Obj *CONST objv[]) {
    ini_buffer_t *arg0 ;
    
    if (SWIG_GetArgs(interp, objc, objv,"p:ini_deleteBuffer buffer ",&arg0, SWIGTYPE_p_ini_buffer_t) == TCL_ERROR) return TCL_ERROR;
    ini_deleteBuffer(arg0);
    return TCL_OK;
}

static int
_wrap_ini_getBuffer(ClientData clientData, Tcl_Interp *interp, int objc, Tcl_Obj *CONST objv[]) {
    ini_buffer_t *arg0 ;
    char *result ;
    
    if (SWIG_GetArgs(interp, objc, objv,"p:ini_getBuffer buffer ",&arg0, SWIGTYPE_p_ini_buffer_t) == TCL_ERROR) return TCL_ERROR;
    result = (char *)ini_getBuffer(arg0);
    Tcl_SetObjResult(interp,Tcl_NewStringObj(result,-1));
    return TCL_OK;
}

static int
_wrap_ini_setBuffer(ClientData clientData, Tcl_Interp *interp, int objc, Tcl_Obj *CONST objv[]) {
    ini_buffer_t *arg0 ;
    char *arg1 ;
    int result ;
    
    if (SWIG_GetArgs(interp, objc, objv,"ps:ini_setBuffer buffer str ",&arg0, SWIGTYPE_p_ini_buffer_t,&arg1) == TCL_ERROR) return TCL_ERROR;
    result = (int )ini_setBuffer(arg0,arg1);
    Tcl_SetObjResult(interp,Tcl_NewIntObj((long) result));
    return TCL_OK;
}

static int
_wrap_ini_new(ClientData clientData, Tcl_Interp *interp, int objc, Tcl_Obj *CONST objv[]) {
    char *arg0 ;
    ini_fd_t result ;
    
    if (SWIG_GetArgs(interp, objc, objv,"s:ini_new name ",&arg0) == TCL_ERROR) return TCL_ERROR;
    result = (ini_fd_t )ini_new(arg0);
    Tcl_SetObjResult(interp,SWIG_NewPointerObj((void *) result,SWIGTYPE_ini_fd_t));
    return TCL_OK;
}

static int
_wrap_ini_open(ClientData clientData, Tcl_Interp *interp, int objc, Tcl_Obj *CONST objv[]) {
    char *arg0 ;
    ini_fd_t result ;
    
    if (SWIG_GetArgs(interp, objc, objv,"s:ini_open name ",&arg0) == TCL_ERROR) return TCL_ERROR;
    result = (ini_fd_t )ini_open(arg0);
    Tcl_SetObjResult(interp,SWIG_NewPointerObj((void *) result,SWIGTYPE_ini_fd_t));
    return TCL_OK;
}

static int
_wrap_ini_close(ClientData clientData, Tcl_Interp *interp, int objc, Tcl_Obj *CONST objv[]) {
    ini_fd_t arg0 ;
    int result ;
    
    if (SWIG_GetArgs(interp, objc, objv,"p:ini_close fd ",&arg0, SWIGTYPE_ini_fd_t) == TCL_ERROR) return TCL_ERROR;
    result = (int )ini_close(arg0);
    Tcl_SetObjResult(interp,Tcl_NewIntObj((long) result));
    return TCL_OK;
}

static int
_wrap_ini_flush(ClientData clientData, Tcl_Interp *interp, int objc, Tcl_Obj *CONST objv[]) {
    ini_fd_t arg0 ;
    int result ;
    
    if (SWIG_GetArgs(interp, objc, objv,"p:ini_flush fd ",&arg0, SWIGTYPE_ini_fd_t) == TCL_ERROR) return TCL_ERROR;
    result = (int )ini_flush(arg0);
    Tcl_SetObjResult(interp,Tcl_NewIntObj((long) result));
    return TCL_OK;
}

static int
_wrap_ini_locateKey(ClientData clientData, Tcl_Interp *interp, int objc, Tcl_Obj *CONST objv[]) {
    ini_fd_t arg0 ;
    char *arg1 ;
    int result ;
    
    if (SWIG_GetArgs(interp, objc, objv,"ps:ini_locateKey fd key ",&arg0, SWIGTYPE_ini_fd_t,&arg1) == TCL_ERROR) return TCL_ERROR;
    result = (int )ini_locateKey(arg0,arg1);
    Tcl_SetObjResult(interp,Tcl_NewIntObj((long) result));
    return TCL_OK;
}

static int
_wrap_ini_locateHeading(ClientData clientData, Tcl_Interp *interp, int objc, Tcl_Obj *CONST objv[]) {
    ini_fd_t arg0 ;
    char *arg1 ;
    int result ;
    
    if (SWIG_GetArgs(interp, objc, objv,"ps:ini_locateHeading fd heading ",&arg0, SWIGTYPE_ini_fd_t,&arg1) == TCL_ERROR) return TCL_ERROR;
    result = (int )ini_locateHeading(arg0,arg1);
    Tcl_SetObjResult(interp,Tcl_NewIntObj((long) result));
    return TCL_OK;
}

static int
_wrap_ini_deleteKey(ClientData clientData, Tcl_Interp *interp, int objc, Tcl_Obj *CONST objv[]) {
    ini_fd_t arg0 ;
    int result ;
    
    if (SWIG_GetArgs(interp, objc, objv,"p:ini_deleteKey fd ",&arg0, SWIGTYPE_ini_fd_t) == TCL_ERROR) return TCL_ERROR;
    result = (int )ini_deleteKey(arg0);
    Tcl_SetObjResult(interp,Tcl_NewIntObj((long) result));
    return TCL_OK;
}

static int
_wrap_ini_deleteHeading(ClientData clientData, Tcl_Interp *interp, int objc, Tcl_Obj *CONST objv[]) {
    ini_fd_t arg0 ;
    int result ;
    
    if (SWIG_GetArgs(interp, objc, objv,"p:ini_deleteHeading fd ",&arg0, SWIGTYPE_ini_fd_t) == TCL_ERROR) return TCL_ERROR;
    result = (int )ini_deleteHeading(arg0);
    Tcl_SetObjResult(interp,Tcl_NewIntObj((long) result));
    return TCL_OK;
}

static int
_wrap_ini_dataLength(ClientData clientData, Tcl_Interp *interp, int objc, Tcl_Obj *CONST objv[]) {
    ini_fd_t arg0 ;
    int result ;
    
    if (SWIG_GetArgs(interp, objc, objv,"p:ini_dataLength fd ",&arg0, SWIGTYPE_ini_fd_t) == TCL_ERROR) return TCL_ERROR;
    result = (int )ini_dataLength(arg0);
    Tcl_SetObjResult(interp,Tcl_NewIntObj((long) result));
    return TCL_OK;
}

static int
_wrap_ini_readInt(ClientData clientData, Tcl_Interp *interp, int objc, Tcl_Obj *CONST objv[]) {
    ini_fd_t arg0 ;
    int *arg1 ;
    int temp ;
    int result ;
    
    if (SWIG_GetArgs(interp, objc, objv,"po:ini_readInt fd value ",&arg0, SWIGTYPE_ini_fd_t,0) == TCL_ERROR) return TCL_ERROR;
    {
        if (Tcl_GetIntFromObj(interp,objv[2],&temp) == TCL_ERROR) {
            return TCL_ERROR;
        }
        arg1 = &temp;
    }
    result = (int )ini_readInt(arg0,arg1);
    Tcl_SetObjResult(interp,Tcl_NewIntObj((long) result));
    {
        Tcl_Obj *o;
        o = Tcl_NewIntObj((int) *(arg1));
        Tcl_ListObjAppendElement(interp,Tcl_GetObjResult(interp),o);
    }
    return TCL_OK;
}

static int
_wrap_ini_readLong(ClientData clientData, Tcl_Interp *interp, int objc, Tcl_Obj *CONST objv[]) {
    ini_fd_t arg0 ;
    long *arg1 ;
    int ivalue ;
    long temp ;
    int result ;
    
    if (SWIG_GetArgs(interp, objc, objv,"po:ini_readLong fd value ",&arg0, SWIGTYPE_ini_fd_t,0) == TCL_ERROR) return TCL_ERROR;
    {
        if (Tcl_GetIntFromObj(interp,objv[2],&ivalue) == TCL_ERROR) {
            return TCL_ERROR;
        }
        temp = (long) ivalue;
        arg1 = &temp;
    }
    result = (int )ini_readLong(arg0,arg1);
    Tcl_SetObjResult(interp,Tcl_NewIntObj((long) result));
    {
        Tcl_Obj *o;
        o = Tcl_NewIntObj((int) *(arg1));
        Tcl_ListObjAppendElement(interp,Tcl_GetObjResult(interp),o);
    }
    return TCL_OK;
}

static int
_wrap_ini_readDouble(ClientData clientData, Tcl_Interp *interp, int objc, Tcl_Obj *CONST objv[]) {
    ini_fd_t arg0 ;
    double *arg1 ;
    double temp ;
    int result ;
    
    if (SWIG_GetArgs(interp, objc, objv,"po:ini_readDouble fd value ",&arg0, SWIGTYPE_ini_fd_t,0) == TCL_ERROR) return TCL_ERROR;
    {
        if (Tcl_GetDoubleFromObj(interp,objv[2],&temp) == TCL_ERROR) {
            return TCL_ERROR;
        }
        arg1 = &temp;
    }
    result = (int )ini_readDouble(arg0,arg1);
    Tcl_SetObjResult(interp,Tcl_NewIntObj((long) result));
    {
        Tcl_Obj *o;
        o = Tcl_NewDoubleObj((double) *(arg1));
        Tcl_ListObjAppendElement(interp,Tcl_GetObjResult(interp),o);
    }
    return TCL_OK;
}

static int
_wrap_ini_writeInt(ClientData clientData, Tcl_Interp *interp, int objc, Tcl_Obj *CONST objv[]) {
    ini_fd_t arg0 ;
    int arg1 ;
    int result ;
    
    if (SWIG_GetArgs(interp, objc, objv,"pi:ini_writeInt fd value ",&arg0, SWIGTYPE_ini_fd_t,&arg1) == TCL_ERROR) return TCL_ERROR;
    result = (int )ini_writeInt(arg0,arg1);
    Tcl_SetObjResult(interp,Tcl_NewIntObj((long) result));
    return TCL_OK;
}

static int
_wrap_ini_writeLong(ClientData clientData, Tcl_Interp *interp, int objc, Tcl_Obj *CONST objv[]) {
    ini_fd_t arg0 ;
    long arg1 ;
    int result ;
    
    if (SWIG_GetArgs(interp, objc, objv,"pl:ini_writeLong fd value ",&arg0, SWIGTYPE_ini_fd_t,&arg1) == TCL_ERROR) return TCL_ERROR;
    result = (int )ini_writeLong(arg0,arg1);
    Tcl_SetObjResult(interp,Tcl_NewIntObj((long) result));
    return TCL_OK;
}

static int
_wrap_ini_writeDouble(ClientData clientData, Tcl_Interp *interp, int objc, Tcl_Obj *CONST objv[]) {
    ini_fd_t arg0 ;
    double arg1 ;
    int result ;
    
    if (SWIG_GetArgs(interp, objc, objv,"pd:ini_writeDouble fd value ",&arg0, SWIGTYPE_ini_fd_t,&arg1) == TCL_ERROR) return TCL_ERROR;
    result = (int )ini_writeDouble(arg0,arg1);
    Tcl_SetObjResult(interp,Tcl_NewIntObj((long) result));
    return TCL_OK;
}

static int
_wrap_ini_listLength(ClientData clientData, Tcl_Interp *interp, int objc, Tcl_Obj *CONST objv[]) {
    ini_fd_t arg0 ;
    int result ;
    
    if (SWIG_GetArgs(interp, objc, objv,"p:ini_listLength fd ",&arg0, SWIGTYPE_ini_fd_t) == TCL_ERROR) return TCL_ERROR;
    result = (int )ini_listLength(arg0);
    Tcl_SetObjResult(interp,Tcl_NewIntObj((long) result));
    return TCL_OK;
}

static int
_wrap_ini_listDelims(ClientData clientData, Tcl_Interp *interp, int objc, Tcl_Obj *CONST objv[]) {
    ini_fd_t arg0 ;
    char *arg1 ;
    int result ;
    
    if (SWIG_GetArgs(interp, objc, objv,"ps:ini_listDelims fd delims ",&arg0, SWIGTYPE_ini_fd_t,&arg1) == TCL_ERROR) return TCL_ERROR;
    result = (int )ini_listDelims(arg0,arg1);
    Tcl_SetObjResult(interp,Tcl_NewIntObj((long) result));
    return TCL_OK;
}

static int
_wrap_ini_listIndex(ClientData clientData, Tcl_Interp *interp, int objc, Tcl_Obj *CONST objv[]) {
    ini_fd_t arg0 ;
    unsigned long arg1 ;
    int result ;
    
    if (SWIG_GetArgs(interp, objc, objv,"pl:ini_listIndex fd index ",&arg0, SWIGTYPE_ini_fd_t,&arg1) == TCL_ERROR) return TCL_ERROR;
    result = (int )ini_listIndex(arg0,arg1);
    Tcl_SetObjResult(interp,Tcl_NewIntObj((long) result));
    return TCL_OK;
}

static int
_wrap_ini_listIndexLength(ClientData clientData, Tcl_Interp *interp, int objc, Tcl_Obj *CONST objv[]) {
    ini_fd_t arg0 ;
    int result ;
    
    if (SWIG_GetArgs(interp, objc, objv,"p:ini_listIndexLength fd ",&arg0, SWIGTYPE_ini_fd_t) == TCL_ERROR) return TCL_ERROR;
    result = (int )ini_listIndexLength(arg0);
    Tcl_SetObjResult(interp,Tcl_NewIntObj((long) result));
    return TCL_OK;
}


static swig_command_info swig_commands[] = {
    { SWIG_prefix "ini_readString", _wrap_ini_readString, NULL},
    { SWIG_prefix "ini_writeString", _wrap_ini_writeString, NULL},
    { SWIG_prefix "ini_createBuffer", _wrap_ini_createBuffer, NULL},
    { SWIG_prefix "ini_deleteBuffer", _wrap_ini_deleteBuffer, NULL},
    { SWIG_prefix "ini_getBuffer", _wrap_ini_getBuffer, NULL},
    { SWIG_prefix "ini_setBuffer", _wrap_ini_setBuffer, NULL},
    { SWIG_prefix "ini_new", _wrap_ini_new, NULL},
    { SWIG_prefix "ini_open", _wrap_ini_open, NULL},
    { SWIG_prefix "ini_close", _wrap_ini_close, NULL},
    { SWIG_prefix "ini_flush", _wrap_ini_flush, NULL},
    { SWIG_prefix "ini_locateKey", _wrap_ini_locateKey, NULL},
    { SWIG_prefix "ini_locateHeading", _wrap_ini_locateHeading, NULL},
    { SWIG_prefix "ini_deleteKey", _wrap_ini_deleteKey, NULL},
    { SWIG_prefix "ini_deleteHeading", _wrap_ini_deleteHeading, NULL},
    { SWIG_prefix "ini_dataLength", _wrap_ini_dataLength, NULL},
    { SWIG_prefix "ini_readInt", _wrap_ini_readInt, NULL},
    { SWIG_prefix "ini_readLong", _wrap_ini_readLong, NULL},
    { SWIG_prefix "ini_readDouble", _wrap_ini_readDouble, NULL},
    { SWIG_prefix "ini_writeInt", _wrap_ini_writeInt, NULL},
    { SWIG_prefix "ini_writeLong", _wrap_ini_writeLong, NULL},
    { SWIG_prefix "ini_writeDouble", _wrap_ini_writeDouble, NULL},
    { SWIG_prefix "ini_listLength", _wrap_ini_listLength, NULL},
    { SWIG_prefix "ini_listDelims", _wrap_ini_listDelims, NULL},
    { SWIG_prefix "ini_listIndex", _wrap_ini_listIndex, NULL},
    { SWIG_prefix "ini_listIndexLength", _wrap_ini_listIndexLength, NULL},
    {0, 0, 0}
};

static swig_var_info swig_variables[] = {
    {0,0,0,0}
};

/* -------- TYPE CONVERSION AND EQUIVALENCE RULES (BEGIN) -------- */

static swig_type_info _swigt__ini_fd_t[] = {{"_ini_fd_t", 0, "ini_fd_t "},{"_ini_fd_t"},{0}};
static swig_type_info _swigt__p_ini_buffer_t[] = {{"_p_ini_buffer_t", 0, "ini_buffer_t *"},{"_p_ini_buffer_t"},{0}};

static swig_type_info *swig_types_initial[] = {
_swigt__ini_fd_t, 
_swigt__p_ini_buffer_t, 
0
};


/* -------- TYPE CONVERSION AND EQUIVALENCE RULES (END) -------- */

SWIGEXPORT(int) Libini_Init(Tcl_Interp *interp) {
    int i;
    if (interp == 0) return TCL_ERROR;
    for (i = 0; swig_types_initial[i]; i++) {
        swig_types[i] = SWIG_TypeRegister(swig_types_initial[i]);
    }
    for (i = 0; swig_commands[i].name; i++) {
        Tcl_CreateObjCommand(interp, (char *) swig_commands[i].name, swig_commands[i].wrapper, swig_commands[i].clientdata, NULL);
    }
    for (i = 0; swig_variables[i].name; i++) {
        Tcl_SetVar(interp, (char *) swig_variables[i].name, "", TCL_GLOBAL_ONLY);
        Tcl_TraceVar(interp, (char *) swig_variables[i].name, TCL_TRACE_READS | TCL_GLOBAL_ONLY, swig_variables[i].get, (ClientData) swig_variables[i].addr);
        Tcl_TraceVar(interp, (char *) swig_variables[i].name, TCL_TRACE_WRITES | TCL_GLOBAL_ONLY, swig_variables[i].set, (ClientData) swig_variables[i].addr);
    }
    return TCL_OK;
}


